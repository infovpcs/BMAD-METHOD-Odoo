# Configuration for Python Build Script

# Path relative to the build-web-agent.py script's directory
asset_root = './bmad-agent/'

# Path relative to the build-web-agent.py script's directory where output files will be created
build_dir = './bmad-agent/build/'

# Path relative to the build-web-agent.py script's directory for the main orchestrator prompt
orchestrator_agent_prompt = './bmad-agent/web-bmad-orchestrator-agent.md'

# Path relative to the build-web-agent.py script's directory for the agent configuration file
agent_cfg = './bmad-agent/web-bmad-orchestrator-agent.cfg.md' 